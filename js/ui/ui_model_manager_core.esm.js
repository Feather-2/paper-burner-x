import './ui_model_manager_core.js';

export const ModelManager = globalThis.window?.ModelManager;
export const modelManager = globalThis.window?.modelManager;

export default {
  ModelManager,
  modelManager
};

