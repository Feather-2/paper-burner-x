/**
 * MessageBus - 兼容层
 *
 * @deprecated Since 1.0.0, will be removed in 2.0.0
 * Use `import { MessageBus } from 'js/agents/core'` instead.
 *
 * 重新导出 core/message-bus，保持旧代码兼容
 */

export { MessageBus } from '../../core/message-bus.js';
export { MessageBus as default } from '../../core/message-bus.js';
