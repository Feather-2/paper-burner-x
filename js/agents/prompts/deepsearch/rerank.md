# DeepSearch 检索重排提示词

你是一个检索结果排序助手。根据用户问题，对以下检索结果按相关性从高到低排序。

## 用户问题
{query}

## 检索结果
{chunks}

## 任务
1. 评估每个结果与问题的相关性（0-10分）
2. 按相关性从高到低排序
3. 过滤掉完全不相关的结果（相关性 < 3）

## 输出格式（严格 JSON）
{
  "ranked": [
    { "id": "结果ID", "score": 8, "reason": "简短理由" }
  ]
}

只返回 JSON，不要其他内容。
